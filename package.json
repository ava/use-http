{
  "name": "use-http",
  "version": "0.1.23",
  "main": "build/main.js",
  "peerDependencies": {
    "react": "^16.8.0"
  },
  "dependencies": {
    "react-scripts": "2.1.8",
    "react-usefetcher": "^0.1.12"
  },
  "devDependencies": {
    "react-dom": "^16.8.6"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && ./src/postBuild.sh",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "prepublishOnly": "yarn build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
