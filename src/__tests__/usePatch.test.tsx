import { usePatch } from '..'

describe('usePatch - general', (): void => {
  it('should be defined/exist when imported', (): void => {
    expect(typeof usePatch).toBe("function")
  })
  console.log('TODO: usePatch')
})
